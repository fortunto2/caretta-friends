# Gradle / build
.gradle/
build/
**/build/
local.properties

# IDE
.idea/
*.iml
*.ipr
*.iws
.fleet/

# Kotlin / Native
.kotlin/

# Xcode / iOS
iosApp/build/
# NB: iosApp.xcodeproj is HAND-MAINTAINED (no xcodegen/tuist) → track project.pbxproj so the app
# name, signing, target settings and the source-file list are versioned. Only user data is ignored.
**/xcuserdata/
**/*.xcworkspace/xcuserdata/

# OS
.DS_Store

# Outputs
outputs/

# SPM cloned packages
iosApp/spm/

# Secrets
.env
supabase/.temp/

# iOS device-build derivedData
iosApp/build-dev/

# iOS device build output (custom derivedDataPath)
iosApp/build-device/
iosApp/build/

# iOS simulator build output (custom derivedDataPath)
iosApp/build-sim/
